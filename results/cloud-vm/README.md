# Cloud-VM timings (not exhibit results)

These CSV/TXT files were produced on a Cursor cloud Linux VM (Intel Xeon under KVM),
not on the author's Windows or MacBook machines.

They are kept only as development artefacts. They must not be cited as course-report
numbers and must not appear in the Bulgarian results tables.

Exhibit timings for the report live in:

- `results/measurements_baseline.csv` / `.txt` (Windows, Ryzen 5 3600)
- `results/measurements_native.csv` / `.txt` (same Windows machine, `-march=native`)

SIMD / xsimd speedups on the author's Windows machine are still unmeasured (`\TODO` in the report).
