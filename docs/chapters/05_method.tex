% IV. Методика на експерименталната проверка.
% Числата от измерванията са в Раздел V, не тук.
\section{Методика на експерименталната проверка}
\label{sec:method}

Измерването отговаря на два въпроса: дава ли реализацията същия резултат като еталон, за който
е известно, че е верен, и колко време и памет отнема. Двете не се смесват, защото по-бърза
стъпка с различен резултат е друга стъпка.

\textbf{Еталон.} За заявките към индекса еталонът е \term{изчерпателното търсене}, което е
точно по определение и е реализирано в проекта; сравнението е по множеството от разстояния, не
по номерата, защото при равни разстояния изборът на съсед не е част от договора. За
съвместяването еталонът е \term{известното преобразувание}, приложено от генератора върху целевия
облак, а за останалите стъпки е свойство, което резултатът трябва да има независимо от
реализацията, например че гаусовото ядро сумира до единица.

\textbf{Данни.} Не се използва външен набор от данни; всичко се произвежда от генератора по
зададено зърно, така че две изпълнения дават един и същ вход. За индекса се ползва равномерен
облак в куб със страна 10 при 10\,000, 50\,000, 200\,000 и 1\,000\,000 точки, защото при
равномерност дървото е най-близо до балансирано. За съвместяването се ползва сцена от равнина,
сфера и куб, общо 70\,000 точки. Само равнина не стига, защото там
съвместяването е недоопределено по две транслации и по една ротация и „сходи“ към произволен
член на цяло семейство решения, което изглежда като успех, докато не се сравни с еталона.
Изходният облак е извадка от 75\,\% от точките, зашумена и преместена, тоест няма общи точки с
целевия.

\textbf{Машина и протокол.} Табл.~\ref{tab:knn}, Табл.~\ref{tab:leafscan} и
Табл.~\ref{tab:icp} са получени на една машина: AMD Ryzen 5 3600 (Zen 2),
6 ядра и 12 нишки, до 3950 MHz, L3 32768 KB, 15,9 GB оперативна памет, AVX2 без AVX-512,
Windows 11 Pro N, g++ 15.2.0 под MinGW-w64, CMake 4.3.2 и Ninja 1.13.2, с флагове
\code{-std=c++20 -O3 -Wall -Wextra}. Втората конфигурация на същата машина е същата плюс
\code{-march=native}; тя не е преносима и се отчита отделно. Сравненията между пътищата там са
с 9 повторения и 2 загрявания, останалите с 5 и 1, и се правят върху едно и също дърво и едни
и същи заявки.

Табл.~\ref{tab:leafscan-simd} и трипътното сравнение скаларен / пакетен / SIMD са получени на
друга машина и не се смесват с горните числа: Intel Xeon (KVM), 4 ядра, 15 GiB оперативна
памет, Linux, g++ 13.3.0, същите флагове. На тази компилация
\code{simd\_leaf\_scan\_available()} връща истина. Подразбиращото се изграждане ползва SSE2;
изграждането с \code{-march=native} ползва AVX. Записаните изпълнения са
\code{results/measurements\_simd.csv} и \code{results/measurements\_simd\_native.csv}.

Отчита се \textbf{минимумът} от повторенията, а медианата и разсейването стоят до него: при
първите изпълнения разсейването достигаше стотици проценти, тоест по ядрата има чужда работа, а
външна намеса може само да \term{удължи} изпълнението.
