% VI. Експериментални резултати и анализ.
% СКЕЛЕТ. Тук няма нито едно число преди измерването да е проведено.
% Всяка таблица се попълва от изхода на измервателния скрипт, не на ръка.
\section{Експериментални резултати и анализ}
\label{sec:results}

Разделът представя резултатите от измерванията по методиката от
Раздел~\ref{sec:method}. Редът следва реда на въпросите: първо съвпадението с еталона,
защото без него измерването на времето няма смисъл, после цената на заявката, после
цената на конвейера като цяло.

\subsection{Съвпадение с еталона}

\TODO{Таблица: стъпка, мярка за съвпадение, резултат, заключение. Попълва се от изхода
на проверката, не на ръка. В текста задължително има коментар и препратка към
таблицата}

\subsection{Цена на заявката за съседи}

\TODO{Таблица: реализация на индекса, брой точки, време за построяване, време за
заявка при зададените стойности на $k$, изразходвана памет}

\TODO{Фигура: време за заявка спрямо броя на точките, за двете реализации на индекса,
с легенда и препратка в текста}

\subsection{Ефект на векторизацията}

\TODO{Таблица: стъпка, скаларна реализация, векторизирана реализация, отношение.
Отношението се изчислява от измерените стойности, не се заявява предварително}

\subsection{Цена на конвейера}

\TODO{Таблица: стъпка от конвейера, дял от общото време. Придружава се с коментар кой
етап доминира и дали това съответства на очакването от Раздел~\ref{sec:design}}

\subsection{Анализ}

\TODO{Анализ на резултатите. Пише се СЛЕД като таблиците са попълнени. Всяко твърдение
тук трябва да сочи към клетка от таблица. Отклоненията от очакваното се отчитат като
отклонения, а не се обясняват с допълнителни предположения}
